I want you to redesign the Character URL UI in my existing "My Dream Women" app.

IMPORTANT:
Do NOT create a new URL system.
The existing per-character URL storage, URL rotation, Chat TXT URL behavior, and related backend logic already exist in the project. Reuse the existing implementation and only improve the UI/UX and URL opening behavior.

I have attached a UI reference image. Follow the 4-part layout shown in the image.

==================================================
1. CHARACTER LIST — MOVE URL BUTTON NEXT TO NAME
==================================================

Currently the 🔗 URL icon is placed at the far-right side of each character row.

This is causing the URL icon to become hidden/overlapped, especially for characters such as:
- மணி
- கௌரி
- அனு

Change this.

Instead of placing the URL icon at the far-right edge, show a compact button immediately to the RIGHT of the character name.

Example:

மணி   [🔗 URLs (3)]
மருமகன் · Young Man

மச்சினிச்சி கயல்   [🔗 URLs (3)]
Computer Engineering

கீதா   [🔗 URLs (2)]
Software Developer

Requirements:
- URL button must be clearly visible.
- URL button must be immediately beside the character name.
- It must NOT be positioned at the screen's far-right edge.
- It must NOT be covered by floating action buttons.
- It must work correctly on small Android screens.
- If the name is long, make the layout responsive instead of allowing overlap.
- Show the number of active/saved URLs dynamically:
  URLs (1), URLs (2), URLs (3), etc.
- If there are no URLs, handle the UI cleanly without breaking the row.

==================================================
2. CHARACTER DETAILS — URL SETTINGS
==================================================

When the user opens a character's details screen, provide a dedicated:

🔗 URL Settings

button/card.

Example:

Profile | Edit | 🔗 URL Settings
                  (3 URLs)

Chat Settings | Photo Styles | Delete

Reuse the existing Character Details screen if one already exists.

Do NOT create duplicate screens unnecessarily.

Tapping "URL Settings" must open the existing URL management screen for that specific character.

==================================================
3. CHARACTER URL SETTINGS
==================================================

Use the existing per-character URL system.

The screen should allow the user to:

- Add URL
- Edit URL
- Delete URL
- Enable / Disable URL
- View active/inactive status
- Manage URL rotation order

Example:

மணி - Saved URLs

1. https://example1.com/mani1
   Active        Edit   Delete

2. https://example2.com/mani2
   Active        Edit   Delete

3. https://example3.com/mani3
   Inactive      Edit   Delete

        + Add URL

Rotation Order:
1 → 2 → 3 → 1 → ...

IMPORTANT:
Do NOT replace the existing database/storage implementation.
Do NOT create duplicate URL records.
Do NOT break existing URL rotation.

==================================================
4. URL CLICK → OPERA BROWSER
==================================================

When the user taps any saved URL:

URL tap
   ↓
Opera Browser
   ↓
URL opens directly

Do NOT open the URL inside an in-app WebView.

Do NOT show a browser chooser.

Do NOT use Android's default browser.

Target Opera Browser specifically.

Before opening:
- Validate that the URL uses http:// or https://.
- Check whether Opera Browser is installed.

If Opera is installed:
→ Open the URL directly in Opera.

If Opera is NOT installed:
Show:

"Opera Browser is not installed. Please install Opera Browser to open this URL."

Do NOT silently fall back to Chrome or another browser.

IMPORTANT:
First inspect the project's existing React Native / Expo / Android architecture and use the correct supported external Intent implementation.

Do not blindly hardcode a package if the installed Opera package differs.
Verify the correct Opera Android package for the app's target/device and use the appropriate package targeting approach.

==================================================
5. CHAT TXT URL ROTATION
==================================================

Keep the existing Chat TXT URL rotation exactly as it currently works.

Example:

Character
→ URL 1
→ URL 2
→ URL 3
→ URL 1
→ ...

Do NOT change:
- URL rotation order
- URL selection logic
- active/inactive logic
- character-specific URL storage

When a URL generated by Chat TXT is tapped, it must also use the same Opera Browser opening behavior.

==================================================
6. DO NOT BREAK EXISTING FEATURES
==================================================

Do NOT modify unrelated functionality.

Especially keep these working:

- Character system
- Character list
- Chat TXT
- URL rotation
- Photo Styles
- Cloudinary
- My Girls
- Character data
- Existing settings
- Existing floating action buttons

The goal is only:
1. Better URL button placement
2. Character-level URL Settings access
3. Clean URL management UI
4. Direct Opera Browser opening

==================================================
7. RESPONSIVE ANDROID UI
==================================================

This app is mainly used on Android phones.

Make sure:
- URL button does not disappear.
- URL button does not overlap the character name.
- Floating action buttons do not cover URL controls.
- Long Tamil character names are handled correctly.
- Small screen widths are supported.
- Existing scrolling behavior remains correct.

==================================================
8. TESTING
==================================================

After implementation, test:

A. Character list
- URL button appears beside character name.
- Test மணி.
- Test கௌரி.
- Test அனு.
- Test a long character name.
- Test character with no URL.
- Test character with multiple URLs.

B. URL Settings
- Add URL
- Edit URL
- Delete URL
- Enable URL
- Disable URL
- Rotation order

C. Opera
- Opera installed → URL opens directly in Opera.
- Opera not installed → correct error message.
- HTTPS URL.
- HTTP URL.
- Saved Character URL.
- Chat TXT generated/rotated URL.

D. Regression
Confirm that existing Chat TXT, Photo Styles, Cloudinary and Character features still work.

==================================================
9. FINAL REPORT
==================================================

After completing the work, report:

1. Exact files changed.
2. Existing components/functions reused.
3. How the URL button was moved beside the character name.
4. How Character → URL Settings works.
5. How Opera Browser is targeted.
6. How Opera-not-installed is handled.
7. How Chat TXT URL rotation was preserved.
8. Testing results.
9. Any remaining issues.

Do not claim tests passed unless you actually ran them.